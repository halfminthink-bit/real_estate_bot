# 🤖 使用しているClaudeモデル

RealEstateBotで使用しているClaudeモデルの一覧です。

---

## 📋 モデル設定

### 設定ファイル

**ファイル**: `projects/setagaya_real_estate/config.yml`

```yaml
llm:
  provider: "anthropic"
  model: "claude-haiku-4-5"  # ← ここで指定
  temperature: 0.7
  max_tokens: 8000
```

### デフォルト値

**ファイル**: `main_orchestrator.py` (136行目)

```python
model=llm_config.get('model', 'claude-sonnet-4-5-20250929')
```

**注意**: 設定ファイル（`config.yml`）に `model` が指定されている場合は、その値が優先されます。

---

## 🎯 各処理で使用するモデル

### 現在の設定（config.ymlより）

**すべての処理で同じモデルを使用**:
- **モデル**: `claude-haiku-4-5`
- **温度**: `0.7`
- **プロバイダー**: Anthropic

### 処理別の詳細

#### 1. アウトライン生成

**ファイル**: `modules/content_generator/generator.py` → `_generate_outline()`

```python
response = self.llm.generate(
    prompt=prompt,
    max_tokens=2000,      # ← アウトラインは2000トークン
    temperature=0.7
)
```

**使用モデル**: `claude-haiku-4-5`（config.ymlで指定）
**最大トークン数**: 2,000
**温度**: 0.7

#### 2. 本文生成

**ファイル**: `modules/content_generator/generator.py` → `_generate_content()`

```python
response = self.llm.generate(
    prompt=prompt,
    max_tokens=8000,      # ← 本文は8000トークン
    temperature=0.7
)
```

**使用モデル**: `claude-haiku-4-5`（config.ymlで指定）
**最大トークン数**: 8,000
**温度**: 0.7

---

## 🔄 モデル変更方法

### 方法1: config.ymlを編集（推奨）

```yaml
# projects/setagaya_real_estate/config.yml
llm:
  provider: "anthropic"
  model: "claude-sonnet-4-5-20250929"  # ← モデル名を変更
  temperature: 0.7
  max_tokens: 8000
```

**利用可能なモデル**:
- `claude-haiku-4-5` - 高速・低コスト（現在の設定）
- `claude-sonnet-4-5-20250929` - バランス型
- `claude-opus-4-20250514` - 高品質・高コスト

### 方法2: コードを直接変更（非推奨）

**ファイル**: `main_orchestrator.py` (136行目)

```python
llm_client = AnthropicClient(
    api_key=api_key,
    model='claude-sonnet-4-5-20250929'  # ← 直接指定
)
```

---

## 📊 モデル比較

| モデル | 速度 | コスト | 品質 | 用途 |
|--------|------|--------|------|------|
| `claude-haiku-4-5` | ⚡⚡⚡ 高速 | 💰 低コスト | ⭐⭐ 標準 | 現在使用中 |
| `claude-sonnet-4-5-20250929` | ⚡⚡ 中速 | 💰💰 中コスト | ⭐⭐⭐ 高品質 | 推奨 |
| `claude-opus-4-20250514` | ⚡ 低速 | 💰💰💰 高コスト | ⭐⭐⭐⭐ 最高品質 | 特別な場合のみ |

---

## 💡 推奨設定

### 本番環境（品質重視）

```yaml
llm:
  provider: "anthropic"
  model: "claude-sonnet-4-5-20250929"
  temperature: 0.7
  max_tokens: 8000
```

### 開発・テスト環境（コスト重視）

```yaml
llm:
  provider: "anthropic"
  model: "claude-haiku-4-5"
  temperature: 0.7
  max_tokens: 8000
```

---

## 🔍 現在の設定確認方法

### ログで確認

記事生成時にログに表示されます：

```
2025-12-02 23:46:31,088 - modules.content_generator.llm.anthropic_client - INFO - Initialized AnthropicClient with model=claude-haiku-4-5
```

### 設定ファイルで確認

```bash
# config.ymlを確認
cat projects/setagaya_real_estate/config.yml | grep -A 3 "llm:"
```

---

## 📝 注意事項

1. **モデル変更時の影響**
   - モデルを変更すると、生成される記事の品質やスタイルが変わる可能性があります
   - 変更後は必ずテスト生成を実行して確認してください

2. **トークン数制限**
   - アウトライン: 2,000トークン（固定）
   - 本文: 8,000トークン（config.ymlで設定可能）

3. **コスト**
   - `claude-haiku-4-5`: 最も低コスト
   - `claude-sonnet-4-5-20250929`: 中コスト
   - `claude-opus-4-20250514`: 高コスト

---

**最終更新**: 2025年12月2日


